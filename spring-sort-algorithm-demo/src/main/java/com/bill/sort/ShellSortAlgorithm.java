package com.bill.sort;

import lombok.extern.slf4j.Slf4j;

@Slf4j
public class ShellSortAlgorithm implements ISortAlgorithm{
    @Override
    public void sort(int[] arr) {
//        int block = arr.length;
//        int tmp;
//        while(block >= 1) {
//            block /= 2;
//
//            // insertion sort
//            for(int i = block; i<arr.length; i++){
//                for(int j=i-block; j>=0; j-=block) {
//                    if(arr[j]>arr[j+block]) continue;
//                    //swap
//                    tmp = arr[j+block];
//                    arr[j+block] = arr[j];
//                    arr[j] = tmp;
//                }
//            }
//        }
    }
}
